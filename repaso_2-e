limite = int(input("Ingresa un número límite: "))
suma_impares = 0

for numero in range(1, limite + 1, 2):
    suma_impares = suma_impares + numero

print(f"La suma de los números impares hasta {limite} es: {suma_impares}")
